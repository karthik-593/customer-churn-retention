# core
pandas>=2.0
numpy
duckdb==1.5.4          # SQL feature engineering straight against the CSVs, no server

# modelling
scikit-learn==1.9.0    # logistic baseline + isotonic calibration
xgboost==3.3.0         # pinned: contact count is a threshold knife-edge, wobbles under multithreading
shap                   # phase C explainability
optuna                 # HPO search (src/hpo.py) — TPE sampler over base12's XGB params

# tracking + registry (local sqlite backend)
mlflow

# serving (phase E)
fastapi
uvicorn[standard]

# notebooks
jupyter

# phase F monitoring is hand-rolled on numpy/pandas: no evidently (drift checks are ~12 hand-rolled tests)